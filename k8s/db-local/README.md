# Manifesto do Postgres para k8s local

Os manifestos nessa pasta serve apenas para rodar o banco de dados `Postgres` em k8s local.

Na nuvem, como `AWS`, não devem ser usados.

Use o serviço `RDS`, preferencialmente.